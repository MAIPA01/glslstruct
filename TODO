- add variable arrays (just add possibility to resize array if it is last variable)

- create struct and layout from glsl string
- add value from glsl string
- add variable with array
- create struct from uniform buffer and from shader storage buffer (check layout type std140, std430, scalar)
- create struct with name from structs in string
- create only structs with names (in array)
- add documentation to parser
- add parser simple tests
- add disable parser (to make library more lightweight)

- writer if ssbo has last element as array make it without size (so it will be a variable array)

// Final
- make tests for every layout type
- check clang-tidy
- format all with clang-format
- check coverage
- check docs
- update readme